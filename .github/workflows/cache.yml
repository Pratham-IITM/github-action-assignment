name: Cache CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-7a28aa9

      - name: prime-cache-7a28aa9
        run: echo "Cache Hit? ${{ steps.cache-step.outputs.cache-hit }}"
